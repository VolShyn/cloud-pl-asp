services:
  webapi:
    build:
      context: .
      dockerfile: dockerfile
    image: sample-webapi-aspnetcore:local
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
